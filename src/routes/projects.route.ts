import fastify, { FastifyInstance } from "fastify";
import { TRouteFunction } from "../utils/fastify-route";

export const ProjectsRouter: TRouteFunction = (fastify: FastifyInstance, _opts, done) => {
    done();
}